Frontend:
1)npm i
2)ng serve

Backend:
1)IIS Express

Database:
1)Create a db named 'CarRental'
2)Run the queries provided under there respective tables i.e., Bill, Category, Product and Users.

Admin credentials:
user: admin@gmail.com
pwd: 123

ps: if a user tries to login immediately after signing in, it will ask for admin approval, so for any user to  login admin has to change the user status from the "manage user" in dashboard.
